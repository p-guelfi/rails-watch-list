<!DOCTYPE html>
<html>
  <head>
    <title>RailsWatchList</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar navbar-light bg-light justify-content-between px-5">
      <a class="navbar-brand"><i class="fa-solid fa-poo"></i></a>
      <div class="form-inline">
        <%= link_to 'My lists', lists_path, class: 'btn btn-primary mr-2' %>
        <%= link_to 'New list', new_list_path, class: 'btn btn-secondary' %>
      </div>
    </nav>
    <%= yield %>
  </body>
</html>
