

<%= simple_form_for @list do |f| %>
  <%= f.input :name, label: 'List Name' %>
  <%= f.input :image_url, label: 'Image URL (optional)' %>
  <%= f.input :photo, as:  :file, label: 'List Name' %>

  <%= f.button :submit, 'Create List' %>
<% end %>
